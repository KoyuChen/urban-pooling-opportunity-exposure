\documentclass[sigconf,anonymous,review]{acmart}

\AtBeginDocument{\providecommand\BibTeX{{Bib\TeX}}}
\setcopyright{none}
\settopmatter{printacmref=true,printccs=true,printfolios=true}
\renewcommand\footnotetextcopyrightpermission[1]{}
\acmConference[KDD '27]{The 33rd ACM SIGKDD Conference on Knowledge Discovery and Data Mining}{August 2027}{San Jose, California, USA}
\acmYear{2027}
\copyrightyear{2027}

\usepackage{mathtools}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{tabularx}
\usepackage{microtype}
\usepackage{url}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}

\newcommand{\F}{\mathcal F}
\newcommand{\M}{\mathcal M}
\newcommand{\ind}{\mathbb I}
\newcommand{\methodname}{\textsc{WorldMatch}}
\newtheorem{proposition}{Proposition}
\newtheorem{lemma}{Lemma}
\newtheorem{theorem}{Theorem}

\title{Learning-Augmented Aggregate Bounds over Hidden Matchings and Privacy-Coarsened Attributes}

\begin{document}

\begin{abstract}
Public releases often preserve one row per transaction while suppressing the
key that joins rows into events and coarsening sensitive endpoint attributes.
Downstream aggregate queries then depend jointly on a latent relation and the
attributes attached to its endpoints. We represent the release by feasible
worlds that jointly assign a nonbipartite matching and categorical endpoint
attributes subject to core--buffer boundary rules, released supports, and
global count constraints. We report attained query endpoints rather than one
imputed graph. Independent attribute supports collapse to ordinary weighted
matching, whereas feasibility under an explicit paired threshold-release
operator is NP-complete even on a complete bipartite outer graph. Learning
enters through an additive matching score whose
positive-affine-invariant regret radius is calibrated on independent markets
with observed relations. Under market-level exchangeability and candidate
support, the restricted feasible-world set, and hence every downstream query
range computed from it, contains the true world with finite-sample probability
at least $1-\alpha$. In a fixed six-pair synthetic benchmark, an illustrative
tight radius for a deliberately query-leaking scorer reduces range width by
95.8\% but retains only 18/120 true matchings. Calibration retains 117/120 and
covers the downstream statistic in 120/120, while width reduction falls to
7.6\%; this is a calibration stress test, not evidence about another domain.
We develop Chicago ride pooling as a flagship application in
which realized co-presence is reported but partner identity and fine geography
are suppressed. Complete-day Chicago estimates remain gated on verification
of the current release operator and are not claimed in this working draft.
\end{abstract}

\ccsdesc[500]{Computing methodologies~Machine learning}
\ccsdesc[300]{Information systems~Data provenance}
\ccsdesc[300]{Theory of computation~Design and analysis of algorithms}
\keywords{uncertain linkage, hidden matching, conformal prediction, privacy coarsening}
\maketitle

\section{Introduction}

Many public datasets preserve outcomes for individual records but remove the
key identifying which records participated in the same event. Sensitive
endpoint attributes may also be generalized or suppressed. Aggregate
quantities such as the composition of matched pairs, referral chains, or
shared-resource groups then depend on two latent objects at once: the relation
and the attributes attached to its endpoints. Selecting one linkage or deleting
rows with missing context creates precision by replacing released uncertainty
with analyst certainty.

Propagating linkage uncertainty alone does not resolve this problem. Candidate
edges can interact with latent endpoint attributes, privacy bucket counts make
attribute completions mutually dependent, and the hidden matching determines
which endpoint attributes enter the query. Separate link and attribute bounds
can therefore combine marginally admissible choices that no single released
database could have produced.

We represent the release by a set of feasible worlds. Each world jointly
specifies a hidden matching and latent endpoint attributes, satisfies every
released value and global coarsening constraint, and covers every core record
exactly once. We compute attained lower and upper endpoints of a declared
aggregate query over this set. These endpoints are conditional on the candidate
supergraph and observation operator; they are not a point reconstruction, and
the closed interval between them need not consist entirely of attainable
values.

Learning is optional and coverage-audited. An edge model supplies a
compatibility score, but node outcomes alone do not identify partner
probabilities. We restrict feasible worlds by matching-level normalized regret
and calibrate the radius on independent markets with observed relations. A
separate supergraph budget permits selected edges outside a baseline graph.
Thus learned sharpening and candidate-support sensitivity remain visible
rather than being folded into one asserted graph.

Chicago ride pooling motivates the full structure. Consider a boundary-complete,
internally consistent cohort with a positive shared-match flag and pooled chain
size two. The public record reports realized co-presence but not
the partner key. Fine geography can also be suppressed. The eventual
application bounds neighborhood-context composition among those customer
transactions. It does not infer rider socioeconomic status, preferences,
durable ties, or all shared runs.

This working version makes three completed contributions. First, it defines
joint feasible-world query semantics, identifies the collapse to weighted
matching, and establishes an NP-complete coupled boundary under a precisely
stated release operator. Second, it gives a matching-level split-conformal
restriction whose finite-sample inclusion event propagates to all predeclared
downstream queries. Third, it implements an exact small-instance oracle and a
numerical joint matching--label program, supplies executable counterexamples,
and freezes a proxy-shift benchmark. A production-scale certified algorithm,
a second relation-truth benchmark, and a complete Chicago run remain explicit
KDD submission gates.

\begin{figure*}[t]
\centering
\begin{tikzpicture}[
  box/.style={draw=black!65, rounded corners=2pt, minimum height=0.68in,
    text width=0.185\textwidth, align=center, inner sep=5pt},
  arrow/.style={-{Latex[length=2mm]}, line width=0.55pt, black!70},
  node distance=0.018\textwidth
]
\node[box] (release) {\textbf{Released rows}\\node outcomes, coarse attributes, privacy cells};
\node[box, right=of release] (worlds) {\textbf{Feasible worlds}\\core--buffer matching, latent labels, global counts};
\node[box, right=of worlds] (learn) {\textbf{Audited restriction}\\market-calibrated score regret and separate $\Gamma$};
\node[box, right=of learn] (query) {\textbf{Query endpoints}\\attained extrema, witnesses, solver status};
\draw[arrow] (release) -- (worlds);
\draw[arrow] (worlds) -- (learn);
\draw[arrow] (learn) -- (query);
\end{tikzpicture}
\caption{The method never fills in one preferred relation. The observation
operator defines a joint world set; learning may restrict that set only through
a separately calibrated rule; optimization then propagates all surviving
ambiguity to the downstream query.}
\Description{Four boxes show released rows flowing to joint feasible worlds,
an audited learned restriction, and aggregate query endpoints.}
\label{fig:overview}
\end{figure*}

\input{sections/related_work}

\section{Problem and Query Semantics}
\label{sec:problem}

\paragraph{Released records and roles.}
Let $V$ index released records and let $G=(V,E)$ be a declared candidate
supergraph. Core records $C\subseteq V$ belong to the target population;
boundary-buffer records $B\subseteq V$ may be partners of core records; and
context-only records $U=V\setminus(C\cup B)$ enter privacy counts but cannot be
selected as partners. A matching $M\subseteq E$ must satisfy
\begin{equation}
 \deg_M(i)=1\quad(i\in C),\qquad
 \deg_M(i)\le1\quad(i\in B),
 \label{eq:corebuffer}
\end{equation}
and contains no buffer--buffer edge. This boundary rule lets a core record use
a counterpart immediately outside the target window without forcing every
buffer record into the target.

\paragraph{Latent attributes and release constraints.}
Each record $i$ has a finite support $\mathcal A_i$ for a latent categorical
attribute $a_i$. Supports may be singletons for released values or larger sets
for coarsened values. In the implemented reference solver, $h(i)$ assigns each
record to one release cell for this modeled attribute. The verified observation
operator supplies rational count bounds
\begin{equation}
 L_{cv}\le \sum_{i:h(i)=c}\ind\{a_i=v\}\le U_{cv}
 \quad(c,v)\in\mathcal K.
 \label{eq:counts}
\end{equation}
More specific threshold or paired-suppression logic can be compiled into the
same feasible-world representation only after its scope and direction have
been verified for the target release. We do not infer a converse suppression
rule from missingness alone.

\paragraph{Feasible worlds and queries.}
A feasible world $F=(M,a)$ satisfies Eqs.~\eqref{eq:corebuffer}--\eqref{eq:counts},
$a_i\in\mathcal A_i$, and any declared attribute-conditioned edge rules. Let
$\F(O,G)$ denote the finite set of such worlds. For a symmetric pair function
$g$, define
the core-transaction query
\begin{equation}
 Q_C(F)=\frac{1}{|C|}\sum_{\{i,j\}\in M}|\{i,j\}\cap C|\,g(a_i,a_j).
 \label{eq:query}
\end{equation}
Core--core edges contribute twice and core--buffer edges once, so the
denominator remains $|C|$. Same-bin composition uses
$g(a_i,a_j)=\ind\{b(a_i)=b(a_j)\}$; signed linear or regression queries replace
$g$ by the corresponding endpoint contribution.

We report
\begin{equation}
 \underline Q=\min_{F\in\F(O,G)}Q_C(F),\qquad
 \overline Q=\max_{F\in\F(O,G)}Q_C(F).
 \label{eq:endpoints}
\end{equation}

\begin{proposition}[Endpoint semantics]
\label{prop:endpoints}
If $\F(O,G)$ is nonempty, both extrema in Eq.~\eqref{eq:endpoints} are attained.
Every feasible world's query value lies between them. If the true world
$F^\dagger$ belongs to $\F(O,G)$, its query value is covered. The proposition
does not imply that every scalar between the endpoints is attainable.
\end{proposition}

\begin{proof}
The domain is a finite set of matchings and finite attribute assignments, so a
real-valued query attains its minimum and maximum. The bracketing and coverage
claims follow from membership. Two feasible worlds can attain only two
distinct values, so finiteness alone provides no interpolation claim.
\end{proof}

\subsection{Observation Operators as Auditable Constraints}
\label{sec:operator}

The world set must encode what the release mechanism actually implies, not a
convenient completion rule. We separate four inputs. \emph{Record supports}
translate released or coarsened values into finite domains. \emph{Count
factors} constrain assignments across every row contributing to a privacy
cell, including context-only rows. \emph{Release factors} state the verified
direction of the mechanism: for example, a visible value may imply that a
count cleared a threshold, while a hidden value need not imply the converse.
Finally, \emph{compatibility factors} specify which attribute pairs allow an
edge. An omitted rule leaves worlds in the set; it does not silently choose a
default completion.

\begin{table}[t]
\caption{Claim boundary induced by the four input layers.}
\label{tab:inputs}
\small
\begin{tabularx}{\columnwidth}{@{}p{0.24\columnwidth}XX@{}}
\toprule
Layer & Supplied object & Failure if misspecified \\
\midrule
Relation support & Supergraph and core/buffer roles & True partner absent \\
Attribute support & Per-record finite domains & True endpoint value absent \\
Observation operator & Count and release factors & Inconsistent world deletion \\
Learned restriction & Score radius and $\Gamma$ budget & Model-induced deletion \\
\bottomrule
\end{tabularx}
\end{table}

This separation yields three different statements. The score-free endpoints
are conditional on the first three layers. A learned endpoint adds the fourth.
A sampling statement additionally requires a design over complete markets.
None of these is a confidence interval merely because two numbers are
reported. The artifact therefore stores the two endpoint witnesses and a
solver-status record alongside every range.

\section{Coupled Endpoint Computation}
\label{sec:computation}

The method must distinguish a useful baseline from the coupled problem.

\begin{lemma}[Independent-support collapse]
\label{lem:collapse}
Suppose (i) $E$ does not depend on latent attributes, (ii) the attribute domain
is Cartesian, $\prod_i\mathcal A_i$, with no cross-record count constraint, and
(iii) the query is pair additive as in Eq.~\eqref{eq:query}. Define
\begin{align*}
 \ell_{ij}&=\min_{a_i\in\mathcal A_i,a_j\in\mathcal A_j}
 |\{i,j\}\cap C|g(a_i,a_j),\\
 u_{ij}&=\max_{a_i\in\mathcal A_i,a_j\in\mathcal A_j}
 |\{i,j\}\cap C|g(a_i,a_j).
\end{align*}
Then the lower endpoint is a minimum-weight core-cover matching with weights
$\ell_{ij}/|C|$, and the upper endpoint is the corresponding maximum-weight
matching with weights $u_{ij}/|C|$.
\end{lemma}

\begin{proof}
For any fixed matching, selected edges share no endpoint. Under a Cartesian
attribute domain, each selected edge's minimizing or maximizing endpoint
assignment can therefore be chosen independently and combined into one global
assignment. Labels of unselected nodes can be completed arbitrarily from their
nonempty supports. Optimizing the resulting edge sums over matchings gives the
stated equalities.
\end{proof}

Thus exact cover, independent missing-bin envelopes, and signed edge objectives
are weighted-matching baselines. The nonseparable problem begins when release
counts or attribute-conditioned feasibility prevent the edgewise choices in
Lemma~\ref{lem:collapse} from coexisting.

\paragraph{Why the joint world changes an endpoint.}
Consider four core records with fixed edges $ab$ and $cd$. Their supports are
$\{A,B\}$, $\{A,C\}$, $\{A,D\}$, and $\{A,E\}$, respectively. Let $A$ form
bin 0, $B,D$ bin 1, and $C,E$ bin 2. With independent supports, the same-bin
query ranges from 0 to 1: choose $(B,C,D,E)$ for the lower witness and all $A$
for the upper witness. Add the single global release constraint
$\#\{i:a_i=A\}\le2$. At most one edge can now have equal labels, so the
attained range contracts to $[0,1/2]$. Optimizing a matching range and an
attribute range separately would retain the impossible upper value 1.

Attributes can also change the relation rather than only its query value. Let
one core $c$ have support $\{A,B\}$ and two singleton buffers $x=A$ and $y=B$.
If edge $cx$ permits only $(A,A)$ and $cy$ only $(B,B)$, a count constraint
fixing $c=A$ selects $x$, while one fixing $c=B$ selects $y$. The implemented
exact oracle reproduces both examples. They isolate the interaction absent
from the independent-support baseline without invoking a privacy mechanism
that has not been verified.

\paragraph{A coupled complexity boundary.}
Fix an integer threshold $k\ge3$. Consider a paired row-release operator that
publishes both endpoint attributes of record $i$ exactly when both its pickup
and drop-off cells contain at least $k$ records; otherwise it publishes neither.
This is an abstract operator, not an assertion about the current Chicago
release.

\begin{theorem}[Paired-suppression feasibility]
\label{thm:hardness}
For every fixed $k\ge3$, deciding whether a feasible attribute assignment and
perfect matching exist under the paired operator is NP-complete, even when the
outer relation is complete bipartite, compatibility depends only on the left
record's pickup attribute, and only left pickup attributes have non-singleton
supports.
\end{theorem}

\begin{proof}[Proof sketch]
Membership in NP follows by checking assignments, cell counts, release states,
compatibility, and matching degrees. For hardness, reduce perfect
three-dimensional matching on equally sized sets $X,Y,Z$. Each $x\in X$ chooses
a latent pickup label $p_x\in Z$. For every $z$, add $k-1$ visible context
anchors to its pickup cell; all anchors share a separate already-large drop-off
cell. Visibility forces $(k-1)+|\{x:p_x=z\}|\ge k$. Since $|X|=|Z|$, every
$z$ is chosen exactly once. Give each $x$ a private low-count drop-off cell so
its two attributes remain paired-suppressed. Match $X$ perfectly to $Y$ and
allow edge $(x,y)$ under label $z$ exactly when $(x,y,z)$ is an input triple.
Feasible worlds are therefore in bijection with perfect 3D matchings. The full
construction, including the $|Z|\ge2$ padding and size bound, is in
Appendix~\ref{app:hardness}.
\end{proof}

The reduction relies on paired row suppression and arbitrary tabular
attribute-conditioned compatibility. The construction does not apply to
endpoint-wise independent suppression and does not establish hardness for a
metric-only edge rule. These boundaries must be checked, not blurred, in any
application.

\paragraph{Threshold compilation.}
The threshold structure also creates an exact algorithmic foothold. Suppose an
operator and every other factor consult a release-cell count only through
predicates $N_c<k$ and $N_c\ge k$; in particular, no constraint or query
distinguishes counts above $k$.

\begin{lemma}[Capped-count sufficiency]
\label{lem:capped}
Replacing every partial or final count $N_c$ by
$\widetilde N_c=\min\{N_c,k\}$ preserves every release predicate, every
feasible world, and the endpoints of every query that depends on selected
relations and attributes but not on the count above $k$.
\end{lemma}

\begin{proof}
The predicates distinguish exactly the states $0,\ldots,k-1$ from the single
state $k$. The update
$\widetilde N_c\leftarrow\min\{k,\widetilde N_c+1\}$ is therefore sufficient
for every assignment sequence. It changes neither the matching nor any
attribute label, so the query is unchanged.
\end{proof}

Compile partner choices, attribute assignments, release factors, and capped
count recursions into a finite factor graph. Let $\tau$ be the treewidth of its
primal graph, $D$ the largest variable domain, and $|\mathcal T|$ the number of
bags in a supplied tree decomposition.

\begin{theorem}[Exact bounded-width inference]
\label{thm:treewidth}
Given a width-$\tau$ tree decomposition, both endpoints of any rational
factor-additive query can be computed, with extremal witness worlds, in
$O(|\mathcal T|D^{\tau+1})$ time and $O(|\mathcal T|D^\tau)$ space, apart from
polynomial arithmetic bit complexity.
\end{theorem}

\begin{proof}
Assign each factor to a bag containing its scope. Min-sum and max-sum messages
enumerate at most $D^{\tau+1}$ assignments per bag and retain the best value
for each separator assignment. Induction from the leaves proves that each
eliminated subtree is optimized conditional on its separator. Root optimization
gives the endpoint; stored choices recover a witness world.
\end{proof}

Theorem~\ref{thm:treewidth} is a certified tractability parameterization, not a
production claim. A KDD-ready system must compile the verified release
operator, exploit temporal components, measure the resulting width, and report
explicit primal/dual gaps when a component is too wide.

\paragraph{Reference formulation.}
The implemented coupled reference program uses binary edge variables $z_e$,
assignment variables $x_{iv}$, and joint edge-label variables $y_{euv}$ only
for ordered pairs $(u,v)$ in an edge's declared compatibility set
$\mathcal P_e$. It enforces Eq.~\eqref{eq:corebuffer},
$\sum_{v\in\mathcal A_i}x_{iv}=1$, the count bounds in
Eq.~\eqref{eq:counts}, and
\[
 \begin{aligned}
 \sum_{(u,v)\in\mathcal P_e}y_{euv}&=z_e,\\
 y_{euv}&\le x_{iu},\qquad y_{euv}\le x_{jv},\\
 y_{euv}&\ge z_e+x_{iu}+x_{jv}-2.
 \end{aligned}
\]
The query is linear in $y$. Context-only records have assignment variables and
enter counts but have no incident selectable edge. An exhaustive enumerator is
the exact oracle on small instances; the sparse mixed-integer program is a
numerical reference backend. It also enforces optional score floors and
supergraph-edge budgets. Exact optima, numerical optima, exact or structural
infeasibility, numerical infeasibility, and unresolved runs are reported as
distinct states; a timeout is unresolved rather than a separate certificate.

The implementation currently models one categorical label and one count-cell
membership per record. The paired pickup/drop-off operator used in
Theorem~\ref{thm:hardness} is a theory instance, not a compiled production
operator. Multiple endpoint attributes require additional variables and count
factors before that theorem can describe an executable application pipeline.

This formulation is not itself the intended KDD algorithmic claim. A scalable
certified decomposition must connect the release operator's factor structure
to production graphs. Until that gate is passed, the program is a correctness
baseline.

\begin{table}[t]
\caption{Algorithmic boundary of the declared world model.}
\label{tab:boundary}
\small
\begin{tabularx}{\columnwidth}{@{}p{0.31\columnwidth}XX@{}}
\toprule
Regime & Endpoint computation & Status \\
\midrule
Fixed attributes & Weighted core-cover matching & Polynomial baseline \\
Independent supports & Two edge-envelope matchings & Lemma~\ref{lem:collapse} \\
Paired threshold coupling & Joint labels and matching & NP-complete \\
Compiled width $\tau$ & Min/max factor-graph DP & $O(|\mathcal T|D^{\tau+1})$ \\
General graph & MILP/decomposition & Gap required \\
\bottomrule
\end{tabularx}
\end{table}

\begin{table}[t]
\caption{Machine-readable solver states and their licensed interpretation.}
\label{tab:status}
\small
\begin{tabularx}{\columnwidth}{@{}p{0.32\columnwidth}XX@{}}
\toprule
State & Source & Licensed statement \\
\midrule
\texttt{EXACT\_OPTIMAL} & Exhaustive oracle & Attained endpoint certified \\
\texttt{NUMERICALLY\_OPTIMAL} & HiGHS incumbent & Numerical endpoint; retain gap \\
\texttt{PROVEN\_INFEASIBLE} & Structural check or enumeration & Declared world set empty \\
\begin{tabular}[t]{@{}l@{}}\texttt{NUMERICALLY\_}\\[-2pt]
\texttt{INFEASIBLE}\end{tabular} & HiGHS status & Numerical evidence only \\
\texttt{UNRESOLVED} & State/time limit or solver failure & No feasibility conclusion \\
\bottomrule
\end{tabularx}
\end{table}

\section{Learning-Augmented Restrictions}
\label{sec:learning}

\subsection{What Node Outcomes Do Not Identify}

Let $\partial M\in\{0,1\}^{V}$ be the incidence vector of a matching. A
released match indicator can reveal $\partial M$ without revealing $M$.

\begin{proposition}[Partner nonidentification]
\label{prop:nonidentification}
If a candidate graph contains distinct matchings $M_1$ and $M_2$ with
$\partial M_1=\partial M_2$, then node match outcomes and node covariates alone
do not identify which of those relations occurred. In particular, an
all-positive incidence vector does not distinguish any two perfect matchings.
\end{proposition}

\begin{proof}
Take two latent worlds with the same released nodes, covariates, and incidence
vector, but relation $M_1$ in one and $M_2$ in the other. Their observations
coincide while their partner edges differ. No function of the released data can
distinguish them without another model or source of relation truth.
\end{proof}

This result does not prohibit structured weak supervision; it fixes its claim
boundary. A model may score complete matchings and enforce mutual exclusion,
but a node-level objective alone cannot validate partner ranks in the regime
of Proposition~\ref{prop:nonidentification}. In our controlled benchmark,
edge scorers are therefore trained on separate synthetic source markets whose
relations are observed. Chicago node outcomes are not used as hidden edge
labels.

\subsection{Calibrating a Matching Set}

Let $s_e$ be any scorer fixed before calibration. With optional buffer nodes,
the score must use core incidence,
\begin{equation}
 S_C(M)=\sum_{e\in M}|e\cap C|s_e.
 \label{eq:corescore}
\end{equation}
Because $\sum_{e\in M}|e\cap C|=|C|$, replacing $s_e$ by $as_e+b$ for $a>0$
changes every feasible score by the same additive constant after scaling.
Let $S^-_d$ and $S^+_d$ be the score extrema over the matching projection of
market $d$'s full feasible-world set, including any attribute-conditioned
compatibility, and define the true matching's normalized regret
\begin{equation}
 R_d=\frac{S^+_d-S_C(M_d^\dagger)}{S^+_d-S^-_d},
 \label{eq:regret}
\end{equation}
with $R_d=0$ for a constant score map. This replaces raw fractions of a
score whose zero has no common meaning across models.

Use $m$ calibration markets, disjoint from scorer training, whose true
matchings are observed. For target miscoverage $\alpha$, let
$k_\alpha=\lceil(m+1)(1-\alpha)\rceil$ and let $\widehat\tau_\alpha$ be the
$k_\alpha$th smallest calibration regret, using the support bound one if
$k_\alpha=m+1$. In a new market retain feasible worlds satisfying
\begin{equation}
 S_C(M)\ge S^+-\widehat\tau_\alpha(S^+-S^-).
 \label{eq:calset}
\end{equation}

Operationally, the procedure is:
\begin{enumerate}[leftmargin=*,nosep,label=\textbf{\arabic*.}]
\item train or otherwise fix $s_e$ using source data disjoint from calibration;
\item for each calibration market solve $S^-$ and $S^+$ over the matching
projection of its declared world set and evaluate the observed true matching;
\item take the finite-sample order statistic of the resulting regrets;
\item in a target market impose Eq.~\eqref{eq:calset}, then solve the lower and
upper program for every predeclared query; and
\item report the radius, candidate-support declaration, witnesses, and solver
status together with the query range.
\end{enumerate}
The expensive object is still a structured optimization oracle. Calibration
adds one scalar nesting rule; it does not turn edge scores into posterior
probabilities.

\begin{theorem}[Market-level calibrated query coverage]
\label{thm:conformal}
Suppose the scorer and set construction are fixed before calibration, the $m$
calibration markets and one new market are exchangeable, and each true world
belongs to its declared candidate and attribute domain. Then
\[
 \Pr\{F^\dagger_{m+1}\in\F_{\widehat\tau_\alpha}(O_{m+1},G_{m+1})\}
 \ge 1-\alpha.
\]
On the same event, the endpoint range over this restricted set simultaneously
covers the true value of every predeclared query.
\end{theorem}

\begin{proof}
Exchangeability makes the new true world's regret rank uniform after random
tie-breaking and conservative under deterministic ties. The probability that
its rank exceeds $k_\alpha$ is at most $\alpha$ by the definition of
$k_\alpha$. Equation~\eqref{eq:calset} is equivalent to regret at most
$\widehat\tau_\alpha$. When the true world is retained, applying
Proposition~\ref{prop:endpoints} to each query yields simultaneous coverage;
no additional union bound over queries is required because they share the same
world-inclusion event.
\end{proof}

The theorem is conditional on candidate support. Given a declared supergraph,
we separately allow at most $\Gamma$ selected edges outside the baseline graph,
\[
 \sum_{e\in E_{\rm super}\setminus E_{\rm base}}z_e\le\Gamma.
\]
If candidate and calibration failures are controlled separately at
$\alpha_G$ and $\alpha_S$, their joint failure is at most
$\alpha_G+\alpha_S$. A supplied supergraph or synthetic calibration alone does
not establish either rate for Chicago.

The two sensitivities form a nested audit rather than a single opaque knob.
Let $\F_{\tau,\Gamma}$ retain worlds with normalized regret at most $\tau$ and
at most $\Gamma$ selected supergraph-only edges.

\begin{proposition}[Nested sensitivity paths]
\label{prop:nested}
If $\tau\le\tau'$ and $\Gamma\le\Gamma'$, then
$\F_{\tau,\Gamma}\subseteq\F_{\tau',\Gamma'}$. Consequently, for the same
query, the lower endpoint cannot increase and the upper endpoint cannot
decrease when either budget is relaxed.
\end{proposition}

\begin{proof}
Every world satisfying the two tighter inequalities satisfies the two looser
ones. Minimization over a superset can only decrease, and maximization can only
increase.
\end{proof}

Reporting the $(\tau,\Gamma)$ path exposes whether apparent precision comes
from score exclusion or candidate exclusion. It also prevents a calibration
claim about $\tau$ from being misread as evidence about graph support.

\section{Controlled Evaluation}
\label{sec:evaluation}

\subsection{Structural and Solver Audit}

The first evaluation asks whether the implemented world semantics exhibit the
claimed interactions before any learned score is used. Table~\ref{tab:audit}
records exact, deterministic cases. The four-record count example is the
worked construction above; the label-conditioned case changes the selected
partner when a global count fixes the core label. A separate seeded audit
generates 250 complete four- or six-core instances with random supports,
count bounds, compatibility tables, and $\Gamma$ budgets. Exhaustive and
HiGHS feasibility statuses agree on all 250; endpoints agree on all 185
feasible cases, while 65 are infeasible. The exhaustive side supplies the
certificate; agreement does not make the floating-point side exact.

\begin{table}[t]
\caption{Exact declared-world checks in the joint reference implementation.}
\label{tab:audit}
\small
\begin{tabularx}{\columnwidth}{@{}p{0.35\columnwidth}XX@{}}
\toprule
Intervention & Before & After \\
\midrule
Global $\#A\le2$ & Independent $[0,1]$ & Coupled $[0,0.5]$ \\
Label-conditioned edge & $c=A\Rightarrow cx$ & $c=B\Rightarrow cy$ \\
$\Gamma$ relaxation & $\Gamma=0:[1,1]$ & $\Gamma=1:[0,1]$ \\
Score restriction & Raw $[0,1]$ & Floor $[1,1]$ \\
Core--buffer target & Two core incidences & Endpoint $1.0$ \\
\bottomrule
\end{tabularx}
\end{table}

The artifact currently has 46 passing unit tests and eight executable
adversarial checks. These cover input validation, state-limit semantics,
positive-affine score invariance, nonattainable interval interiors, signed
objectives, and node-marginal nonidentification. They support software behavior
for declared finite inputs, not the correctness of an external observation
operator.

\subsection{Matching-Set Calibration under Deliberate Query Leakage}

\paragraph{Design.}
We use a deterministic, disjoint source/calibration/test split. Forty source
markets directly supervise logistic edge scorers with observed true edges; no
node-label learner is evaluated here. Forty-nine target-style markets calibrate
Eq.~\eqref{eq:regret}, and 120 held-out markets are used for final evaluation.
Each market has six hidden pairs, fully observed binary endpoint attributes,
and a complete candidate graph. There is no privacy-count coupling in this
experiment, so it isolates the matching-set calibration layer.

Target-free features measure route, time, and duration similarity. A
query-leaking diagnostic additionally sees the same-SES edge contribution
that defines the downstream query. The source homophily generating probability
is 95\%, while calibration and test use 55\%, deliberately shifting that
circular feature. The nominal matching-set coverage is 90\%. Radius 0.05 is
an illustrative stress point, not a selected or calibrated baseline. The
committed program uses numerical HiGHS endpoints; independent exhaustive
enumeration over all 10,395 perfect matchings per market reproduces the
reported counts and widths.

\begin{table*}[t]
  \caption{Fixed held-out synthetic markets: coverage cannot be read from width alone.}
  \label{tab:conformal}
  \small
  \centering
  \begin{tabular}{@{}lrrrrrrrr@{}}
    \toprule
    & \multicolumn{3}{c}{Calibrated restriction} && \multicolumn{3}{c}{Arbitrary radius $0.05$} & \\
    \cmidrule{2-4}\cmidrule{6-8}
    Scorer & Match cov. & Query cov. & Width red. && Match cov. & Query cov. & Width red. & Point MAE \\
    \midrule
    Target-free & 95.8\% & 100\% & 3.8\% && 30.0\% & 83.3\% & 70.6\% & 0.122 \\
    Query-leaking diagnostic & 97.5\% & 100\% & 7.6\% && 15.0\% & 24.2\% & 95.8\% & 0.308 \\
    \bottomrule
  \end{tabular}
  \vspace{2pt}
  \parbox{0.94\textwidth}{\footnotesize The score-free range covers the query
  in every test market and has mean width 0.661. Calibrated radii are 0.341
  (target-free) and 0.504 (query-leaking). ``Match cov.'' tests whether the entire
  hidden matching remains eligible; ``Query cov.'' is weaker because an
  excluded matching's scalar value can still fall between the endpoints.
  The observed 100\% values mean 120/120, not a strengthened theoretical
  coverage level.}
\end{table*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.88\textwidth]{figures/conformal_tradeoff.pdf}
\caption{The illustrative tight radius occupies the upper-left region: large
width reduction but poor retention or coverage. Market-level calibration moves
both scorers across the nominal 90\% line, but most apparent precision
disappears. Thin paths show the exact-enumeration radius grid from 0 to 1;
arrows connect the radius-0.05 stress point to the calibrated radius for the
same scorer.}
\Description{Two scatter plots compare range-width reduction against true
matching retention and downstream-query coverage. Tight uncalibrated points
have high width reduction and low coverage; calibrated points have high
coverage and low width reduction.}
\label{fig:tradeoff}
\end{figure*}

\paragraph{Result.}
The query-leaking radius-0.05 stress point looks strongest if evaluated only by
width: it removes 95.8\% of the score-free ambiguity. It retains 18/120 true
matchings and covers the downstream query in 29/120 markets. Calibration
detects the shifted shortcut. Its radius expands to 0.504, retaining 117/120
true matchings and covering the scalar query in 120/120 markets, while width
reduction falls to 7.6\%. The target-free score needs a smaller radius but also
gains little calibrated precision. This single seed and market size do not
establish robustness. The result is narrower: calibration makes the coverage
price of sharpening measurable rather than rewarding circular precision.

\paragraph{Software audit.}
The benchmark validates matching-level calibration only. It does not validate
node weak supervision, latent-attribute inference, privacy-count coupling,
candidate support, or transfer to Chicago. Multi-seed, size, sparsity,
coarsening, and candidate-omission sweeps remain required evidence.

\section{Chicago Flagship Application: Evidence Gate}
\label{sec:chicago}

Chicago's public Transportation Network Provider table reports whether each
customer transaction shared a vehicle with a separately booked customer and
reports the pooled chain size \citep{chicagodata2026}. It does not publish the
partner or shared-trip identifier. Conditional on boundary completeness and
reporting consistency, matched $K=2$ transactions therefore induce a hidden
nonbipartite perfect matching. The application target is the
neighborhood-context composition of these realized two-transaction matches.

\begin{table}[t]
\caption{Chicago observation-operator contract in this working draft.}
\label{tab:chicago-contract}
\small
\begin{tabularx}{\columnwidth}{@{}p{0.30\columnwidth}X@{}}
\toprule
Status & Object and licensed use \\
\midrule
Released & Transaction-level shared-match flag and reported chain size \\
Latent & Which two eligible transactions are partners; suppressed fine context \\
Unverified & Current threshold logic, contributing-cell population, boundary support \\
Prefix sample & Schema and mechanics checks only \\
Full extraction & Mechanism-conditional contextual-composition endpoints, if all gates pass \\
Never licensed & Person-level SES, preferences, durable ties, or inferred row-level links \\
\bottomrule
\end{tabularx}
\end{table}

The production analysis is not yet licensed. The committed data are a
deterministic two-character trip-ID-prefix sample. Conditional on retaining one
member of a two-record relation, that design omits the counterpart with
probability approximately $255/256$ under independent prefix retention. It is
suitable for schema checks, not partner reconstruction or endpoint bounds.
Moreover, the accessible historical tract-suppression clarification is tied to
legacy datasets; its unchanged operation in the 2025/2026 release has not been
verified. We therefore do not encode the historical rule as current fact.

The complete application requires all transactions contributing to each
privacy cell, boundary-buffered matched rows, a necessary-condition candidate
supergraph, the correct tract vintage, and full solver status by component.
Primary comparisons will include complete cases, community-area aggregation,
independent attribute envelopes, one point imputation, spatiotemporal proxy
grouping, score-free coupled endpoints, and calibrated restrictions only where
independent partner truth supports them. No Chicago composition or January
2026 policy sign is reported in this draft.

\section{Limitations and Ethical Considerations}

The endpoint guarantee is conditional on the declared world set. Candidate
omissions, an incorrect release operator, nonexchangeable calibration markets,
or unmodeled attribute supports can remove truth before optimization. The
split-conformal result is marginal across complete markets, not conditional on
every market and not transferable from simulation by assertion. Numerical
MILP optima are not exact mathematical certificates; production reporting must
retain primal/dual gaps and unresolved states.

The Chicago unit is a customer transaction, not a stable person. Census
attributes describe neighborhood context, not rider income or race. Releasing
one extremal latent assignment could facilitate reidentification, so the
intended artifact reports only aggregate endpoints, support diagnostics, and
solver certificates. It must not publish imputed partner links, trip IDs, or
latent fine-geography assignments. Institutional review and the City's data
terms must be resolved before any nonpublic data are used.

\section{Generative AI Usage}

Generative AI assisted code drafting, adversarial counterexample search, and
language editing. Every mathematical claim, experiment, citation, and code path
remains subject to author verification. No generated text or synthetic output
is treated as evidence about Chicago.

\section{Conclusion}

Privacy coarsening can hide a relation and its endpoint attributes at the same
time. The appropriate output is then a query over feasible worlds, not a
preferred reconstructed graph. Independent attribute supports reduce to
weighted matching; global release coupling creates the substantive new
problem. Matching-level conformal calibration can prevent a learned scorer
from receiving credit for false precision, but it may also reveal that little
safe sharpening is available. The remaining work is decisive rather than
cosmetic: verify the current operator, turn the bounded-width route into a
production certified algorithm, validate on another ground-truth relation,
and run the full Chicago application.

\clearpage
\bibliographystyle{ACM-Reference-Format}
\bibliography{references}

\clearpage
\appendix

\section{Reproducibility and Claim Status}

The artifact includes the complete benchmark generator, split-conformal
calibrator, exact small-instance matching oracle, sparse numerical reference
solver, fixed seeds, result tables, and executable adversarial examples. Hidden
pair truth is read only in the source-training, calibration, or held-out-test
stage declared by the benchmark split. The earlier two-day weak-node-score
pilot is retained as failure-analysis provenance but is not a headline result.

Current completed claims are limited to Propositions~\ref{prop:endpoints},
\ref{prop:nonidentification}, and \ref{prop:nested},
Lemmas~\ref{lem:collapse} and \ref{lem:capped},
Theorems~\ref{thm:hardness}, \ref{thm:treewidth}, and
\ref{thm:conformal}, 46 passing declared-input unit tests, eight adversarial
checks, the seeded 250-instance solver audit, and the controlled results in
Table~\ref{tab:conformal}. The
scalable decomposition, current Chicago observation
operator, non-Chicago relation benchmark, and complete-day Chicago estimates
remain open gates and cannot be inferred from the appendix.

\section{Full Proof of Theorem~\ref{thm:hardness}}
\label{app:hardness}

Define the decision problem as follows. Records have pickup and drop-off time
labels, finite attribute supports, and an observed symbol that is either a
visible attribute pair or missing. A world chooses both attributes. The paired
operator releases both exactly when both selected endpoint cells have count at
least $k$. Target records form left and right sets and must be perfectly
matched; context records cannot be matched. An explicitly represented table
$\Gamma_{xy}(p_x)$ determines compatibility. A certificate is polynomially
checked by computing all counts and matching degrees, so the problem is in NP.

Reduce perfect 3D matching with $|X|=|Y|=|Z|=q$. If needed, add one disjoint
forced triple so $q\ge2$ without changing the answer. Create target records
$r_x$ and $r_y$ and the complete outer graph $X\times Y$. All $r_x$ share one
pickup time and have support $Z$. Each $r_x$ has a private drop-off time and a
singleton drop-off support; its observed pair is missing. Each $r_y$ has
private endpoint times, singleton supports, and a missing observed pair.

For each $z\in Z$, create $k-1$ context anchors with the common pickup time and
singleton pickup support $\{z\}$. All anchors share a drop-off time and a
singleton label unused by targets, and are visibly released. The anchor
drop-off count is $q(k-1)\ge k$. If $n_z=|\{x:p_x=z\}|$, each $z$-anchor pickup
count is $(k-1)+n_z$; visibility is therefore equivalent to $n_z\ge1$.
Because $\sum_z n_z=q$, every $n_z=1$. Missing target outputs are consistent:
every $x$ has a unique count-one drop-off cell, and every $y$ has unique
count-one endpoint cells.

Finally set $\Gamma_{xy}(z)=1$ exactly for input triples $(x,y,z)$. A feasible
world matches every $x$ and $y$ once and assigns every $z$ once, so its selected
triples form a perfect 3D matching. Conversely, a perfect 3D matching selects
the corresponding $p_x$ and $(x,y)$ edges; all anchor counts equal or exceed
$k$, all targets remain suppressed, and every selected edge is compatible.
For fixed $k$ the construction has $O(q)$ records, $O(q^2)$ support and outer-
edge entries, and at most an $O(q^3)$ explicit compatibility table. The
reduction is polynomial.

\end{document}
