# Adversarial Issue Ledger

Issues below were independently reconstructed from the frozen repository, executable reproductions, official field definitions, or primary literature. “Open” means the claim must not appear in an abstract or conclusion in its current form.

| ID | Claim or component | Severity | Evidence | Minimal counterexample | Required repair | Repair status | Independent audit status | Effect on abstract and conclusions |
|---|---|---|---|---|---|---|---|---|
| AO-01 | The matched cohort represents only service-chain compatibility, not verified co-presence | Critical | The official Chicago TNP manual defines `Shared Trip Match` as whether the passenger shared the vehicle with a separately booked passenger at any point | A released row with `Shared Trip Match=Y` already certifies some co-presence even though the partner is hidden | Redefine the missing object as partner identity/hidden matching; distinguish node-level realized co-presence from edge-level identity | Repaired in KDD working manuscript and repository documentation | Official definition and target implication independently verified | Abstract and conclusion now use realized co-presence with hidden partner identity |
| AO-02 | `Trips Pooled=2` plus matched rows induces the paper's exact-cover cohort without qualification | Critical | Current public rows include match/authorization/run-count contradictions; run IDs are absent and a run may cross midnight | A hidden counterpart outside the extracted day or with inconsistent fields leaves an observed matched node unpairable | State and empirically audit run closure, cardinality, literal nulls, and consistency; include cross-boundary retrieval | Conditional statement repaired; production cohort open | Official definitions, live cross-tabs, and boundary counterexamples independently audited | No unconditional claim that every retained row has exactly one retained partner |
| AO-03 | Public rows are individual riders | Major | The reporting manual defines a trip as one customer transaction, potentially transporting additional people under that booking | A family booking and a separate single-person booking produce two trip rows but more than two riders | Use customer transaction, booking, or customer trip unless party size is observed | Open | Official definition verified | Remove rider-level exposure language |
| AO-04 | Released centroid distances form a common-resolution physical measurement | Critical | Tracts may be suppressed and the released centroid may correspond to a coarser community area | Two nearby pickups represented at different aggregation levels can have released centroids more than 4 km apart | Use polygons/uncertainty regions or a common geography; keep point-distance screens as sensitivity restrictions | Open | Observation-operator counterexample complete | Do not call the spatial screen a physical outer set |
| AO-05 | A downloaded historical day is a final complete population | Major | The current City notice permits publication at generally 99%+ receipt, late additions, and revisions to past periods | A late provider batch adds a missing run component after endpoints were computed | Pin row/schema revision before and after extraction, then repeat after a later release and compare | In-pull snapshot fingerprint implemented and tested; cross-release stabilization open | Four deterministic snapshot-contract tests pass; official notice verified | “Complete” must always mean complete within one pinned public revision until stabilized |
| ID-01 | The raw range covers the true pairing | Critical | Code applies time/space/cosine screens and a global degree cap of 16 | A true edge can pass local screens and still be removed when both endpoints lose the edge under greedy degree truncation | Report graph-conditional coverage; construct nested candidate sensitivities or a bounded omitted-edge model | Open | Code path and deletion mechanism verified | “Coverage” must always be qualified by candidate support |
| ID-02 | Equation (2) is a latent-edge likelihood | Critical | The implemented objective multiplies overlapping node-marginal terms; node events share edges | With one latent edge, the product gives positive probability to `(Y1,Y2)=(1,0)`, which is impossible | Call it a composite marginal scoring loss, or replace it with a coherent matching-aware likelihood | Open | Formal counterexample complete | Remove “likelihood” and all posterior/probability interpretations of edge scores |
| ID-03 | Node labels identify edge compatibility rankings | Critical | Node probabilities depend on incidence sums of edge hazards | On a four-cycle, hazards `(t,L-t,t,L-t)` give identical node marginals for every `t` but reverse the preferred perfect matching | Treat ranking as a learned inductive assumption and validate against independent edge truth; do not identify it from node labels | Open | Formal nullspace construction complete | Abstract may claim weakly supervised scoring only, not edge recovery |
| ID-04 | The displayed min/max interval is the sharp attainable set | Major | Finite matching set yields attained endpoints but not scalar convexity | A four-cycle with two feasible matchings can attain only `H=0` and `H=1`; `0.5` is inside the displayed range but unattainable | Say “sharp endpoints” or “convex-hull range”; reserve “identified set” for the discrete image | Audit/code terminology repaired; frozen manuscript unchanged | Formal counterexample and executable check complete | Replace “sharp interval/set” wording throughout before any submission |
| ID-05 | The `rho` restriction is stable and comparable across score maps | Critical | Feasibility uses `sum(w_e z_e) >= rho W*` | Adding a constant to all scores changes membership while rankings are unchanged; monotone squaring can reverse which matching maximizes total score | Freeze one predeclared reference score range, calibrate matching-level regret across markets, and use exactly the same rationalized scorer in calibration and optimization | Method repair complete: `FixedScoreRange`, exact additive scoring, directed rational rounding, and fixed-reference nestedness are implemented; Chicago calibration absent | Nestedness counterexample, exact integration tests, and independent theorem audit complete | Raw same-`rho` horse races remain prohibited; only assumption-explicit calibrated sets may be claimed |
| ID-06 | Complete-case ACS joins preserve the true pairing target | Critical | Solver requires observed SES for every retained endpoint and then exact-covers only those nodes | If one endpoint's tract is suppressed, deleting it can strand or re-pair its observed partner | Optimize lower/upper edge contributions jointly over matchings and admissible missing-context completions | Partial: explicit Cartesian-support envelopes and signed endpoint solver implemented; suppression coupling/data absent | Proof and exhaustive tests complete for fixed graph/population/denominator and independent supports | No claim about all matched trips from complete-case bounds |
| ID-07 | The 30-minute/4-km/7-km/direction graph contains every true co-present pair | Critical | These are analyst thresholds, not implications of the reporting manual | A two-hour trip overlaps a later 30-minute trip whose start is 60 minutes later and pickup is 8 km away | Build the inferential graph from necessary possible-overlap constraints; move spatial screens to sensitivities | Open | Minimal co-presence counterexample complete | Candidate graph must be called analyst-declared, not physically identified |
| ID-08 | Calendar-day graph construction preserves hidden pairs | Critical | Code groups by `event_day` and forbids cross-date edges | Transactions 23:50–00:20 and 00:05–00:25 can be a matched (K=2) pair but appear on different start dates | Use extraction buffers and one global graph; audit outer-boundary nodes | Open | Cross-midnight counterexample complete | No daily exact-cover claim before boundary repair |
| ID-09 | Missing tract values can be treated as independent income-bin choices without losing operator information | Major | Current-linked City documentation describes endpoint-marginal threshold cells and paired coarsening; it also lists nonprivacy causes of blanks | Several edgewise maxima can require incompatible counts, while a blank pickup can belong to a high pickup cell when its drop-off cell is low | Compile documented one-way implications over all contributing trips; keep blank-cause/converse states explicit | Generic DNF compiler complete; Chicago null-cause adapter and production data open | Projection/replay, lifecycle, and 1,000 randomized parity checks pass; source semantics independently audited | Independent envelopes remain a baseline; no suppression-aware Chicago result yet |
| CA-01 | The current exposure statistic has a policy ITT interpretation | Critical | `H` is conditional on matching, pair eligibility, and observed context, all potentially affected by treatment | Treatment can change the selected matched/observed cohort while every individual's potential exposure is unchanged | Define a fixed trip-level reported-transaction outcome and optimize the actual regression coefficient; reserve causal language | Partial: fixed-design FWL weights and signed edge envelopes implemented; causal design/data absent | Algebraic identity and executable tests complete | Remove “ITT on opportunity exposure” until the target and design are repaired |
| CA-02 | Optimizing day/block endpoints bounds the policy coefficient | Critical | Regression contrasts can contain positive and negative weights and hidden pairs may cross blocks | Independently minimizing each day's `H` can combine matchings that are not jointly feasible and need not minimize the coefficient | Use sign-aware endpoint selection for factored blocks or one global signed edge objective when edges cross blocks | Partial: generic signed endpoint solver implemented; global production graph absent | Formal reduction and FWL proposition complete | Current policy-effect equation cannot support a policy sign |
| CA-03 | Current files support `authorization/all` and `matched/all` | Critical | `where_for()` in the complete-day downloader adds `shared_trip_authorized = true` | Unauthorized trips never enter the downloaded denominator | Maintain an all-trip policy extract and a separate linkage extract | Open | Code predicate verified | Remove `/all` estimates until the all-trip pipeline exists |
| CA-04 | Policy treatment is determined by origin zone alone | Critical | Official January 2026 rule applies when either pickup or drop-off lies in a covered zone and schedules differ by weekday/weekend | A control-origin trip dropping inside the zone is treated by law but coded untreated | Encode pickup-or-dropoff exposure, day-of-week schedule, and old/new zone status | Open | Official notice verified | Proposed DDD treatment is currently misspecified |
| CA-05 | A (K=2)-conditional composition summarizes all shared service | Major | Chain size is a post-policy outcome and (K>2) runs have non-clique overlap | A cross-bin pair becomes part of a three-transaction run after policy and disappears from the (K=2) cohort | Limit the estimand name to two-transaction matched runs or model larger-run overlap uncertainty | Open | Selection counterexample complete | No general claim about pooling composition |
| VA-01 | The locked generator supplies scientific validation of edge recovery | Critical | Generator encodes income in tract/coordinates and makes matched status separable through trip attributes; prefix data have no edge truth | The repository smoke test gives the transparent rule Brier `0` and negative relative AI improvement; high performance is generator-dependent | Add untouched generator families, ablations, stress tests, and external or realistic edge truth | Partial: UCI block-1 topology boundary and FEBRL4 method-fit audit added; learned external calibration absent | External hashes/topology/leakage controls and exact six-pair oracle verified | Legacy pilot remains software/logic evidence only |
| VA-02 | The 22-feature specification is a locked primary model | Major | `DESIGN_LOCK.json` locks the earlier full specification; the 22-feature ablation was created after inspecting its circularity on the same holdout day | Selecting the ablation after seeing truth can improve reported ranking/coverage by construction | Label it post-hoc diagnostic; create a new frozen benchmark before any primary claim | Open | Repository chronology verified | Remove “primary locked” language for the ablation |
| VA-03 | Rule-versus-AI at equal `rho` isolates the learned model | Major | Rule score still includes tract/area equality while the 22-feature learned model is described as geography-equality-free | The comparator has access to the exact feature family being criticized in the diagnostic AI | Use common feature restrictions and invariant ambiguity radii, or drop the horse race | Open | Code feature map verified | Do not claim fair comparative sharpening |
| CO-01 | Solver failures or floating-point termination certify exact infeasibility/optimality | Major | Error handling formerly rewrapped limits as infeasible; HiGHS can also treat tiny objective differences as zero | A feasible timed-out instance was conflated with infeasibility; an objective gap of $10^{-9}$ produced the wrong endpoint with a nominal zero MIP gap before normalization | Separate structural/exhaustive certificates, numerical optima, numerical infeasibility reports, limits, and errors; retain gaps/messages | Repaired in legacy failure propagation and new interface; SciPy objectives are normalized and marked `NUMERICALLY_OPTIMAL`, never exact-certified | Regression and adversarial small-instance tests pass | Full-day outputs must report numerical qualification and may not call floating-point MILP output an exact proof |
| CO-02 | The score-constrained solver is computationally validated for production scale | Major | A generic release compiler and bounded 23-case profile now exist, but no run-closed complete-day schedule exists | Degree-five 14-record profile times out; fixed-width four-cycles still encode SUBSET-SUM through the score resource | Compile boundary-safe complete days, report measured density/width/factors/frontier/runtime/memory, and predeclare exact/outer/numerical fallback | Partial: compiler, exact DP, weak hardness, outward score certificate, and bounded profile implemented; production validation open | 96 unit checks, locked 34-case benchmark, lifecycle parity, and capacity harness pass | May claim a verified reference algorithm and conditional/measured synthetic profile, not production scalability |
| CO-03 | One exact-cover node set is sufficient for boundary-safe extraction | Major | A boundary-buffered production graph needs core nodes covered exactly once while buffer nodes are optional and must not create buffer-only objective contributions | A core transaction whose partner starts just outside the target day needs a buffer partner; forcing every buffer node into the cover changes the target, while deleting buffers loses the true edge | Implement core degree `=1`, buffer degree `<=1`, and exclude buffer-only edges from the target; audit outer boundaries separately | Reference temporal solver repaired: core `=1`, buffer `<=1`, context unmatched, and optional-only edges rejected; production compiler/boundary audit open | Role semantics and exhaustive small-instance tests pass | May claim the reference role semantics, but not a boundary-complete production extraction |
| NV-01 | The methodological ingredients are individually novel | Critical | Prior work covers linkage ranges, budgeted matching, structured conformal prediction, bounded-width CSP, pathwidth/vertex separation, knowledge compilation, and Chicago pooling | A generic MILP, DNF label duplication, OBDD monitor, or matching-level conformal set is decomposable into prior modules | Limit contribution to the release-coupled temporal state/invariant, weak-hardness boundary, exact witnesses, and outward score containment; treat compiler/locality as auditable enablers | Conditional repair strengthened by compiler, profile, and partial external audit; full-corpus/production validation absent | Primary-source matrix, locked audits, UCI/FEBRL boundary, and independent circuit counterexamples complete | Broad “first/new framework” language prohibited; state the precise integrated increment and gates |
| EM-01 | Complete Chicago structural and policy evidence exists | Critical | Frozen repository contains a 1/256 prefix and synthetic outputs; full-day results are explicitly reserved | Almost every potential partner is absent from the prefix, so it cannot support a latent matching | Acquire complete boundary-safe extracts and pass all predeclared gates | Open | File audit and fetcher behavior verified | No Chicago exposure estimate or policy conclusion may appear |
