# Legacy AI4Science / weak-node-score pilot

This snapshot contains the superseded `model/` and `integration/` pipelines,
old reports, manifests, and the offline-pilot runner. It is retained because the
failed edge-identification and circularity diagnostics informed the current
claim boundary.

The archived code is not run by current CI and may refer to its former paths.
Use Git history at the archival commit for exact historical reproduction. No
result in this directory is headline EventFrontier evidence.
