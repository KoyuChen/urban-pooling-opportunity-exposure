# Next empirical gate

The real-data engineering gate is now candidate support, not API access.
Priority order:

1. obtain or independently validate a necessary candidate-supergraph rule;
2. add the City's release-operator count constraints to exploit information
   beyond timestamps and coarsened centroids;
3. obtain restricted Shared Trip ID or another partner-truth sample for recall
   and Chicago coverage calibration;
4. only then promote the sensitivity graph from an analyst scenario to a
   statistical support claim.
