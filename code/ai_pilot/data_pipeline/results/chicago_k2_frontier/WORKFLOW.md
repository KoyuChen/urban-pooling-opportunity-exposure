# Workflow

Live job: `chicago-k2-frontier`, run `33468343107`.

The workflow installs the locked numerical environment, runs deterministic
frontier and partitioned-fetch tests, queries the official Chicago release,
reconciles a 611-row candidate universe in 14 timestamp partitions, solves the
radius and Gamma curves, and uploads aggregate evidence. Both the unit and live
jobs completed successfully.
