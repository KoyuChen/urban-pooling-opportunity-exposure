# Redaction

No raw Chicago row, raw trip ID, pair endpoint ID, or selected matching witness
is committed or included in the aggregate report. The live extractor hashes
ID sets for reconciliation and serializes only counts, query hashes, solver
status, and aggregate endpoints.
