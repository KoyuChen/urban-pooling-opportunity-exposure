# Numerical status

All resolved graph-cover and endpoint optimizations returned
`OPTIMAL_NUMERICAL_MILP` with MIP gap zero and passed independent integer
constraint replay. These are solver-qualified numerical results, not exact
rational certificates. The fare query is unresolved rather than numerically
approximated because public fare is missing on six candidate rows.
