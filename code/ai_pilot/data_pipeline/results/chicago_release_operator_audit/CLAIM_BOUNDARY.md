# Claim boundary

## Supported

For the fixed released 15-minute core, the live extraction is
snapshot-relative and server-count closed for both the 611 literal public
K=2 temporal candidates and the 50,405 public trips contributing to their
released endpoint bins.

The strict-positive-overlap graph retains 18,462 edges after separating 5,812
boundary-touch-only edges and has an optimal core cover. A second optimal
strict cover changes all 60 core assignments with every public row field held
fixed. This certifies public pairing nonidentification.

The candidate endpoint mask audit is exact: Community Area presence equals
released-coordinate presence row-for-row at pickup and dropoff. The release
operator supplies no additional necessary deletion among unmeasured-distance
edges.

## Not supported

The audit does not validate the City's private production code, identify the
cause of any public blank, reconstruct latent tract cells, authenticate a
blank-to-LOW converse, recover Shared Trip ID, identify a true partner, measure
partner recall, recursively close buffer runs, or turn a centroid radius into a
necessary spatial compatibility rule.

Every public blank uses an uninformative TRUE release clause unless independent
operator evidence is supplied. This run emits zero LOW literals and leaves
`city_implementation_validated=false`.
