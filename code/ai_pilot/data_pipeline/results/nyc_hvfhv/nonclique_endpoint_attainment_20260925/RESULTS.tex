% Generated aggregate mechanism audit; no raw rows or memberships.
\paragraph{Why the public non-clique comparison is an endpoint null.}
Across all 192 frozen endpoint--restriction checks, the pair or clique family contains a feasible world using the same selected-buffer subset as an ordered endpoint witness. Ordered events still add projected subsets in 1/24 common-$q$ cells versus cliques and 3/24 versus pairs. Thus event partitions may expand without changing selected-buffer support, and support may expand without changing these additive endpoints.
This mechanism audit uses no new window, support, or query and does not imply family equivalence.
